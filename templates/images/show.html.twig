{% extends 'base.html.twig' %}

{% block title %}Images{% endblock %}

{% block body %}
    <h1>Images</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ image.nom }}</td>
            </tr>
            <tr>
                <th>Image</th>
 ²              <td><img src="{{ asset('images/products/' ~ image.nom) }}" alt={{ image.nom }} height="100px" width="100px"></td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_images_index') }}">back to list</a>

    <a href="{{ path('app_images_edit', {'id': image.id}) }}">edit</a>

    {{ include('images/_delete_form.html.twig') }}
{% endblock %}
